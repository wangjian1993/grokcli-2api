## 整体架构

还是使用monorepo

- 根目录直接放置项目 相关东西可以放到`packages`下即

- packages
- src
- pnpm-workspace.yaml
- vite.config.ts
- ....

去掉turbo打包的相关依赖 直接使用vite打包

组件库完全使用`antdv-next` 完全抛弃`shadcn` 不兼容的地方使用antdv-next组件/手搓实现

依赖管理仍然使用`catalog`体系

oxc系列需要还原为eslint+prettier

## 基础架子

- 权限体系/store等保留
- utils等保留
- usevbenform如果有依赖shadcn的地方 替换为原生/antdv实现
- usevbenmodal/drawer替换为antdv-next的实现 兼容之前的api
- vxe table相关功能需要完整保留
- menu替换为原生/antdv实现 不兼容的地方使用手搓实现

## 设计 token / 主题系统(最大的隐藏底座)

- 使用antdv-next的设计/css变量体系 抛弃原框架的 design-tokens 这套 CSS 变量层

## vee-validate + zod —— useVbenForm 的真正引擎

- 改用 a-form 原生校验(rules):更"纯 antd",但 98 个业务文件的 schema 校验写法可能要适配。

## effects/layouts(12,629 行)整个外壳,不只是 menu

- 完全重构 使用antdv-next组件/手搓

## effects/common-ui(97 文件/9,260 行)远不止 form/modal

- 重写/用 antdv 替代

## 图标体系

- 保留图标方案 即iconify+离线图标+svg图标 但是lucide-vue-next需要去除/替代
